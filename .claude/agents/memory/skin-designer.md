# Memoria — skin-designer

Estados por celda: cumple / migrado / pendiente / exento

| Fecha      | Juego         | clasico   | neon      | retro     | Notas / exenciones                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------- | ------------- | --------- | --------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-02 | rocas         | migrado   | migrado   | migrado   | Motor en `lib/games/asteroids.ts` (no `rocas.ts`). Infra creada: `lib/games/skins.ts`, selector genérico en `PlayerHud` (props opcionales `skin`/`onSkinChange`) + CSS `.skin-picker`/`.skin-btn`. Contraste WCAG validado (todas ≥4.5:1, fondos L≤0.005). Retro: powerup/indicador en ámbar `#ffb000` para distinguir por tono sobre fósforo verde. Ojo: hook auto-formatea (prefer-const) — `palette` debe seguir siendo `let`.                                                                                                         |
| 2026-08-02 | caida         | pendiente | pendiente | pendiente | Sin skins aún; recordar `drawNextPreview` recibe paleta.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-02 | bloque-buster | migrado   | migrado   | migrado   | `BLOQUE_BUSTER_SKINS` en `lib/games/bloque-buster.ts`. Ojo: el "clásico" del juego YA es vector-neón (reemplazó el spritesheet) — la skin `neon` se diferencia con synthwave saturado sobre violeta `#0b0217` y glow 1.5x; `retro` = 8 bits apagado sobre ámbar-negro `#140d02`, glow 0.35x. Paleta con `bloques: Record<string,string>` (claves de niveles) + `brillo`/`paleta`/`bola`/`glow` (factor sobre blur base). Contraste WCAG validado: fondos L≤0.0044, elementos ≥5.11:1. Sonidos MP3 sin skin (no visuales, no es exención). |
| 2026-08-02 | serpentina    | migrado   | migrado   | migrado   | `SerpentinaPalette` + `SERPENTINA_SKINS` en `lib/games/serpentina.ts`; selector/persistencia en `SerpentinaPlayer`. Contraste WCAG validado (cabeza/cuerpo/fruta ≥4.5:1, grid ≤3:1, fondos L≤0.05). Distinguibilidad por tono: neón cabeza magenta vs cuerpo cian; retro cabeza verde fósforo vs cuerpo ámbar. **Exención**: frutas con sprites PNG (`fruits.png`, drawImage) quedan fuera del skin; solo el círculo placeholder usa la paleta (`fruta`).                                                                                 |
