# Memoria — skin-designer

Estados por celda: cumple / migrado / pendiente / exento

| Fecha      | Juego         | clasico | neon    | retro   | Notas / exenciones                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------- | ------------- | ------- | ------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-02 | rocas         | migrado | migrado | migrado | Motor en `lib/games/asteroids.ts` (no `rocas.ts`). Infra creada: `lib/games/skins.ts`, selector genérico en `PlayerHud` (props opcionales `skin`/`onSkinChange`) + CSS `.skin-picker`/`.skin-btn`. Contraste WCAG validado (todas ≥4.5:1, fondos L≤0.005). Retro: powerup/indicador en ámbar `#ffb000` para distinguir por tono sobre fósforo verde. Ojo: hook auto-formatea (prefer-const) — `palette` debe seguir siendo `let`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-09 | caida         | migrado | migrado | migrado | `CaidaPalette` + `CAIDA_SKINS` en `lib/games/caida.ts` (`fondo`/`grid`/`brillo`/`piezas[1..8]`). Se eliminó el export `CAIDA_COLORS` (sin consumidores) y `GRID_LINE`; `drawBlock` recibe `palette`. `drawNextPreview(canvas, piece, skin = "clasico")` — el preview solo se repinta en `onNext`, así que el wrapper guarda la última pieza en `nextPieceRef` y la redibuja en el efecto de `skin`. `draw()` ahora pinta `palette.fondo` (en `clasico` es `#000`, idéntico al `background` del CSS → regresión cero). Contraste validado: fondos L≤0.0034, piezas ≥4.58:1, grid ≤1.21:1. Distinguibilidad de tono entre tetrominós ≥30° (neón) / ≥24° (retro); la tuerca `N` se distingue por saturación baja (≤0.17) como el gris original. **Nota**: `clasico` conserva O/L a Δh=10° porque es la paleta original tal cual (no se ajusta). Confirmado de nuevo el hook prefer-const: dejó `const palette` y hubo que revertirlo a `let`. |
| 2026-08-02 | bloque-buster | migrado | migrado | migrado | `BLOQUE_BUSTER_SKINS` en `lib/games/bloque-buster.ts`. Ojo: el "clásico" del juego YA es vector-neón (reemplazó el spritesheet) — la skin `neon` se diferencia con synthwave saturado sobre violeta `#0b0217` y glow 1.5x; `retro` = 8 bits apagado sobre ámbar-negro `#140d02`, glow 0.35x. Paleta con `bloques: Record<string,string>` (claves de niveles) + `brillo`/`paleta`/`bola`/`glow` (factor sobre blur base). Contraste WCAG validado: fondos L≤0.0044, elementos ≥5.11:1. Sonidos MP3 sin skin (no visuales, no es exención).                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-02 | serpentina    | migrado | migrado | migrado | `SerpentinaPalette` + `SERPENTINA_SKINS` en `lib/games/serpentina.ts`; selector/persistencia en `SerpentinaPlayer`. Contraste WCAG validado (cabeza/cuerpo/fruta ≥4.5:1, grid ≤3:1, fondos L≤0.05). Distinguibilidad por tono: neón cabeza magenta vs cuerpo cian; retro cabeza verde fósforo vs cuerpo ámbar. **Exención**: frutas con sprites PNG (`fruits.png`, drawImage) quedan fuera del skin; solo el círculo placeholder usa la paleta (`fruta`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-09 | frogger | cumple | cumple | cumple | Juego nuevo (sustituye al slug `ranaria`, borrado de la tabla `games`). `FroggerPalette` + `FROGGER_SKINS` ya venían del port; contrato `init`/`setSkin`, wrapper y selector correctos sin cambios. **Contraste corregido en las 3 skins**: (a) `tronco` era casi invisible sobre el río (1.94 / 3.06 / 2.87 :1) → aclarado a `#c88b4a` / `#b98cf0` / `#d69a45` (ahora ≥4.9:1) con su `troncoVeta` reajustada; (b) **neón: la rana y `coches[0]` eran el MISMO magenta `#ff2fd6`** → el magenta queda reservado a la rana y los coches pasan a `["#ff6a3d","#f5ff00","#00f0ff","#b06bff"]`; (c) retro: rana y tortugas eran el mismo verde (Δh=0°) → tortugas a ámbar-oliva `#c8d400` (Δh=57°); (d) clásico: tortugas de verde a verde azulado `#25c2a8` para separarlas de la rana (Δh 21°→45°); (e) `ranaOjo` ilegible sobre el cuerpo (clásico 1.33:1, retro 1.46:1) → rana clásica a `#2fe83f` y ojo retro a ámbar oscuro `#5c3400`; (f) `camionCabina` neón 4.11→`#9d7bff`, `ranaPata` neón a violeta `#8f3dff` (distinto tono del cuerpo magenta), `tortugaSumergida` con alfa subido (clásico 0.28→0.42, neón/retro →0.32) para que se perciba sin dejar de avisar del peligro. Criterios aplicados: jugables (coches/camión/tronco/tortuga/rana/bocas/HUD/barra de tiempo) ≥4.5:1 contra SU fondo de zona; `carril` decorativo ≤3:1; detalles internos (patas, ojos, vetas, escamas, ruedas) ≥1.5:1 contra su elemento padre; `tortugaSumergida` ≥1.8:1; barra de tiempo con los 3 estados separados por tono (Δh ≥27°). Sin exenciones: Frogger no usa sprites de imagen, todo es canvas 2D. |
